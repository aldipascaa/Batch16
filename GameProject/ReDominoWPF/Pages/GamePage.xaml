<Page x:Class="ReDominoWPF.Pages.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F0F0F0"
      Loaded="Page_Loaded">
    <Grid Background="DarkSeaGreen">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with player turn and restart button -->
        <Border Grid.Row="0" Background="White" BorderBrush="#CCC" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="6">
                <TextBlock x:Name="TurnTextBlock" FontSize="18" FontWeight="Bold" Foreground="#1F4E78"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="GameStatusTextBlock" FontSize="14" Foreground="#666" Margin="20,0,0,0"
                           VerticalAlignment="Center"/>
                <Button x:Name="RestartButton" Content="Restart" Margin="16,0,0,0" Width="60" Height="40"
                        Background="#FF9800" Foreground="White" FontWeight="Bold" Click="RestartButton_Click"
                        HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <Button x:Name="PlayPauseButton" Content="Music on" Margin="16,0,0,0" Click="PlayPauseButton_Click" Width="100" Height="40"/>
            </StackPanel>
        </Border>

        <!-- Computer Dominos (face-down, no numbers) -->
        <GroupBox Grid.Row="1" Margin="6" Padding="6" BorderThickness="0">
            <StackPanel>
                <TextBlock Text="Computer's Dominos"
                   HorizontalAlignment="Center"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10" />

                <WrapPanel x:Name="ComputerDominosPanel" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <!-- Board -->
        <GroupBox Grid.Row="2" Margin="6" Padding="16" BorderThickness="0">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Center" VerticalAlignment="Center">
                <WrapPanel x:Name="BoardPanel" Orientation="Horizontal"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Player Hand (clickable dominos) -->
        <GroupBox Grid.Row="3" Margin="6" Padding="6" BorderThickness="0">
            <StackPanel>
                <TextBlock
                   HorizontalAlignment="Center"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10">
                    <Run  Text="{Binding playerName}"/>
                    <Run Text=" Dominos"/>
                </TextBlock>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <WrapPanel x:Name="PlayerDominosPanel" Grid.Column="0"
                           Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <Button x:Name="DrawButton" Content="Draw" Width="100" Height="45" 
                            Background="#2196F3" Foreground="White" FontWeight="Bold" Click="DrawButton_Click"
                            Cursor="Hand" Margin="0,0,0,8"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
       
        </GroupBox> 
    </Grid>
</Page>